import { defineConfig } from 'astro/config';

// Update `site` to your real domain once it's registered —
// it's used for the sitemap and canonical URLs.
export default defineConfig({
  site: 'https://example.com',
  output: 'static',
});
